def tables_to_latex(text: str) -> str:
    # Placeholder: keep text; extend rules if needed.
    return text

def formulas_to_latex(text: str) -> str:
    # Placeholder for formula heuristics; return text unchanged for now.
    return text
